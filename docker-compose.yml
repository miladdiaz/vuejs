version: "3.9"
services:
  nginx:
    container_name: nginx
    image: nginx
    ports:
      - "80:80"
    networks:
      - nginx
  vue:
    container_name: vue
    image: vue
    networks:
      - vue
      - nginx

networks:
  nginx:

  vue:

